const ContainerContext = React.createContext();

export const ProviderContainer = ContainerContext.Provider;
export const ConsumerContainer= ContainerContext.Consumer;

export default ContainerContext;